# ALLOG UI 정합성 점검 정리 (구현 완료본)

**"모름" 4개(3-1~3-4) 제외 전부 로컬에서 구현 완료했습니다. git엔 커밋/푸시하지 않았습니다.**
안드로이드 번들 컴파일 확인 완료(200 OK). 실기기 테스트는 네이티브 모듈(캘린더피커/클립보드) 때문에
Expo Go로는 불가 — 개발 빌드(EAS 또는 로컬 Android SDK)가 필요하며, 이번엔 빌드를 생략하기로 하셨습니다.

**구현 중 발견된 이슈 2가지:**
1. **공개 그룹 초대코드**: 백엔드가 공개 그룹에는 초대코드 발급 자체를 막아놨어요(`issueInviteCode`가 409 CONFLICT "공개 그룹은 초대 코드가 필요 없어요" 반환). 그룹명·정원 개인화 문구는 복원했지만, 코드 표시는 백엔드 정책상 구현 불가 — 백엔드 쪽에서 정책을 바꾸지 않는 한 프론트만으로는 못 함.
2. **NavIcons.jsx 삭제**: 자동 승인이 안 돼서(권한 정책) 실제 삭제는 못 했어요. 안 쓰이는 죽은 코드라 동작엔 영향 없으니, 원하시면 직접 지우시거나 다음에 요청해주세요.
3. **웹 프리뷰**: 네이티브 모듈 설치 과정에서 `react-native-web`이 꼬여서 브라우저 미리보기가 깨졌어요. 실제 타겟(안드로이드)엔 영향 없어서 그대로 뒀습니다(요청하신 대로).

---

- 비교 대상: `feature/fullstack-integration`(cb905b1) ↔ `origin/main`(haewon060310/ALLOG-FRONTEND, 최종 디자인 기준)
- 제외 범위: 내 그룹, AI 코칭, 랭킹, 인증(카메라) 화면
- 이 문서는 정리본이며, 실제 코드는 아직 수정하지 않았습니다.

---

## 1. 수정 필요 (결정 완료 — 바로 작업하면 되는 것)

### 1-1. 그룹 초대 화면 — 코드 복사 기능이 아예 없음
`app/group/invite.jsx`
`expo-clipboard`가 설치조차 안 되어 있어서 초대코드를 복사할 방법이 없음. 복사 버튼 + 클립보드 연동 추가.

### 1-2. 그룹 초대 화면 — "참여 방법" 안내 텍스트 삭제됨
`app/group/invite.jsx`
"1. 탐색 화면에서 '코드로 참여하기'를 눌러요 / 2. 위 코드를 입력하면 바로 참여돼요" 2단계 안내 복원.

### 1-3. 탐색 > 그룹 상세 — 헤더 배지에 서버 원문(raw status) 노출
`app/explore/group/[groupId].jsx`
`RECRUITING` 같은 서버 status 문자열이 그대로 노출됨. 사용자용 라벨("DAY N" 등)로 매핑.

### 1-4. 온보딩 완료 — 조사 누락
`app/onboarding/complete.jsx`
"하트는 그룹 참가에**만** 사용돼요" — "만" 복원.

### 1-5. 마이페이지 — 프로필 편집 "저장하기"가 실제로 저장을 안 함
`app/my/edit-profile.jsx`
`updateMyProfile()`(`PATCH /api/v1/users/me`)가 이미 있는데 미연결. 화면에 API 연결 + 실제 프로필 로드.

### 1-6. 온보딩 — 첫 화면(STEP 1) 뒤로가기 버튼이 동작 안 함
`app/onboarding/basic-info.jsx`
`router.replace()`로 진입해서 navigation history가 없음. STEP 1에서 뒤로가기 숨기거나 대체 동작 필요.

### 1-7. ✅[결정] Pretendard 폰트 전체 반영
`app/_layout.jsx`
폰트 파일을 앱 루트로 옮기고 `_layout.jsx`에 로드 코드 추가. **→ 지금 반영하기로 결정.**

### 1-8. ✅[결정] 브랜드 컬러 — 그린(#14453a)으로 통일
검정(`bg-ink`)으로 바뀐 것들을 원래 그린으로 되돌림. 대상: 홈 "인증하러 가기", 탐색 필터/카테고리칩/"직접 그룹 만들기", 마이페이지 아바타, 그룹 참가하기, 그룹생성·참가완료 체크서클, 탐색>그룹상세 뒤로가기 버튼. **→ 기존(그린) 색깔대로 유지하기로 결정.**

### 1-9. ✅[결정] 그룹 생성 완료 — 공개 그룹도 초대코드 표시
`app/group/created.jsx`
현재는 비공개 그룹만 초대 화면에서 코드를 보여주고, 공개 그룹 생성자는 코드를 볼 곳이 없음. **→ 공개 그룹도 코드 표시하기로 결정.** 그룹명·정원 개인화 문구도 함께 복원.

### 1-10. ✅[결정] 탐색 > 그룹 상세 — 초대코드 칩 복원
`app/explore/group/[groupId].jsx`
헤더의 초대코드 칩(🔗, 누르면 초대 화면 이동)을 다시 추가. **→ 복원하기로 결정.**

### 1-11. ✅[결정] 온보딩 — 생년월일 네이티브 달력 피커 추가
`app/onboarding/basic-info.jsx`
`@react-native-community/datetimepicker` 설치 + 캘린더 아이콘에 실제 피커 연결. **→ 피커 추가하기로 결정.** (네이티브 모듈이라 Expo Go로는 테스트 불가, 커스텀 개발 빌드 필요할 수 있음)

### 1-12. ✅[결정] 마이페이지 — 프로필 편집에 "관심 카테고리" 편집 UI 신규 추가
`app/my/edit-profile.jsx`
origin/main에도 원래 없던 기능이지만 새로 구현하기로 결정. **→ 이번에 추가.**

### 1-13. ✅[결정] 하단 탭바 아이콘 — 새 SVG 세트로 최종 확정
`app/(tabs)/_layout.jsx`
새 아이콘이 최종 디자인으로 결정. 더 이상 안 쓰는 `src/components/nav/NavIcons.jsx`는 죽은 코드이므로 정리(삭제) 필요.

---

## 2. 확정됨 (코드 변경 불필요 — 현재 상태 유지)

### 2-1. 탐색 — 모집 상태 필터 제거 유지
`app/(tabs)/explore.jsx`
전체/모집중/정원충족 필터를 되살리지 않고 지금처럼 제거된 상태 유지하기로 결정. 손댈 필요 없음.

### 2-2. 그룹 생성 — 인증 시간대 고정값("매일 오후 11시") 확정
`app/group/create.jsx`
커스텀 시간대 편집기를 복원하지 않고 고정값으로 확정. 손댈 필요 없음.

---

## 3. 아직 결정 안 됨 (여전히 "모름" — 계속 논의 필요)

### 3-1. 온보딩 완료 — 하트 환급 조건 다이어그램 복원 여부
`app/onboarding/complete.jsx`
초록 조건박스 → 화살표 → 주황 보상박스("그룹 공동 성공률 80% + 개인 달성률 70%")가 사라지고 "취소 시 자동 환급" 문구로 대체됨.
**→ 결정 필요.**

### 3-2. 리워드 화면 — 필터/정렬/가격/교환 버튼, 완전 제거 vs 비활성화
`app/(tabs)/reward.jsx`
지금은 컨트롤 자체가 안 보이는 상태. 비활성화 상태로 남겨서 API 붙으면 바로 살릴 수 있게 할지, 지금처럼 완전히 없앨지.
**→ 결정 필요.**

### 3-3. 하트 안내 화면 — 인터랙티브 하트 획득 리스트 유지 여부
`app/heart-event.jsx`
루틴인증/인스타팔로우/친구초대/친구응원 4개 항목이 사라지고 정적 안내 카드로 대체됨. 기능 자체를 없앨지, 백엔드 붙을 때까지 임시로만 숨긴 상태로 둘지.
**→ 결정 필요.**

### 3-4. 탐색 — "AI 추천" → "마감임박" 리브랜딩 확정 여부
`app/(tabs)/explore.jsx`
타이틀·알고리즘·문구가 전부 "남은 자리 최소순"으로 바뀜. 이대로 확정할지, AI 추천 기능을 복원해야 할지.
**→ 결정 필요.**

---

## 참고 — 검토했지만 문제 없는 것

- **CoachMascotButton**: origin/main 최신 캐노니컬 소스와 정확히 일치. 조치 불필요.
- **인증/온보딩 7개 화면** (Start, Login, Signup 2종, Onboarding 3종, OnboardingShell): 전부 1:1 포팅 확인.
- **마이페이지 서브 3개 화면** (알림/고객센터/약관): 전부 일치.
- **그룹 참가 플로우** (join, join-complete, waiting-room, Confetti): 전부 일치.
- **탐색 필터 / 그룹 생성 — 카테고리 목록**: origin/main과 완전히 동일.
- **온보딩 — 전화번호 인증(SMS)**: origin/main 원본도 동일하게 mock 상태. 포팅 문제 아님.

---

*제외된 화면: 내 그룹(`app/(tabs)/group.jsx`), AI 코칭(`app/ai.jsx`), 랭킹(`app/ranking.jsx`), 인증 카메라 플로우(`app/verify/**`)*
